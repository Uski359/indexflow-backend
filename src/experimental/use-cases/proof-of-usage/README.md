## Proof of Usage (Experimental)

This folder contains a deterministic, explainable Proof-of-Usage validation module exposed via a public API for pilot and grant validation.

It is read-only, uses placeholder interaction data, and does not modify or depend on core indexer, listener, or database logic.
